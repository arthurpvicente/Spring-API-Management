spring.application.name=apimanagement
server.port=${PORT:8081}

spring.profiles.active=${SPRING_PROFILES_ACTIVE:teste}
spring.jpa.open-in-view=false

jwt.secret=${JWT_SECRET}
jwt.expiration=86400000

twilio.account.sid=${TWILIO_ACCOUNT_SID:placeholder}
twilio.auth.token=${TWILIO_AUTH_TOKEN:placeholder}
twilio.whatsapp.from=${TWILIO_WHATSAPP_FROM:whatsapp:+14155238886}
twilio.signature.validation.enabled=${TWILIO_SIGNATURE_VALIDATION:true}
